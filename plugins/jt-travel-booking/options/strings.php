<?php
	if (!empty ($translate[travel_days_text])) {
		$travel_days_text = $translate[travel_days_text];
	}
	else {
		$travel_days_text = 'days';
	}

	if (!empty ($translate[travel_starts_from_text])) {
		$travel_starts_from_text = $translate[travel_starts_from_text];
	}
	else {
		$travel_starts_from_text = 'Starts from:';
	}

	if (!empty ($translate[travel_info_text])) {
		$travel_info_text = $translate[travel_info_text];
	}
	else {
		$travel_info_text = 'Info';
	}

	if (!empty ($translate[travel_daily_program_text])) {
		$travel_daily_program_text = $translate[travel_daily_program_text];
	}
	else {
		$travel_daily_program_text = 'Daily Program';
	}

	if (!empty ($translate[travel_provisions_text])) {
		$travel_provisions_text = $translate[travel_provisions_text];
	}
	else {
		$travel_provisions_text = 'Provisions';
	}

	if (!empty ($translate[travel_hotels_text])) {
		$travel_hotels_text = $translate[travel_hotels_text];
	}
	else {
		$travel_hotels_text = 'Hotels';
	}

	if (!empty ($translate[travel_things_to_do_text])) {
		$travel_things_to_do_text = $translate[travel_things_to_do_text];
	}
	else {
		$travel_things_to_do_text = 'Things to do';
	}

	if (!empty ($translate[travel_booking_info_text])) {
		$travel_booking_info_text = $translate[travel_booking_info_text];
	}
	else {
		$travel_booking_info_text = 'Booking Info';
	}

	if (!empty ($translate[travel_video_text])) {
		$travel_video_text = $translate[travel_video_text];
	}
	else {
		$travel_video_text = 'Video';
	}

	if (!empty ($translate[travel_departures_text])) {
		$travel_departures_text = $translate[travel_departures_text];
	}
	else {
		$travel_departures_text = 'Departures';
	}

	if (!empty ($translate[travel_photo_gallery_text])) {
		$travel_photo_gallery_text = $translate[travel_photo_gallery_text];
	}
	else {
		$travel_photo_gallery_text = 'Photo Gallery';
	}

	if (!empty ($translate[travel_book_this_tour_text])) {
		$travel_book_this_tour_text = $translate[travel_book_this_tour_text];
	}
	else {
		$travel_book_this_tour_text = 'Book This Tour';
	}

	if (!empty ($translate[travel_book_now_text])) {
		$travel_book_now_text = $translate[travel_book_now_text];
	}
	else {
		$travel_book_now_text = 'Book now and we will contact you as soon as possible to confirm your booking.';
	}

	if (!empty ($translate[travel_first_name_text])) {
		$travel_first_name_text = $translate[travel_first_name_text];
	}
	else {
		$travel_first_name_text = 'First Name';
	}

	if (!empty ($translate[travel_last_name_text])) {
		$travel_last_name_text = $translate[travel_last_name_text];
	}
	else {
		$travel_last_name_text = 'Last Name';
	}

	if (!empty ($translate[travel_email_text])) {
		$travel_email_text = $translate[travel_email_text];
	}
	else {
		$travel_email_text = 'Email';
	}

	if (!empty ($translate[travel_phone_number_text])) {
		$travel_phone_number_text = $translate[travel_phone_number_text];
	}
	else {
		$travel_phone_number_text = 'Phone Number';
	}

	if (!empty ($translate[travel_number_of_persons_text])) {
		$travel_number_of_persons_text = $translate[travel_number_of_persons_text];
	}
	else {
		$travel_number_of_persons_text = 'Number of Persons';
	}

	if (!empty ($translate[travel_departure_text])) {
		$travel_departure_text = $translate[travel_departure_text];
	}
	else {
		$travel_departure_text = 'Departure';
	}

	if (!empty ($translate[travel_comments_text])) {
		$travel_comments_text = $translate[travel_comments_text];
	}
	else {
		$travel_comments_text = 'Additional Comments';
	}

	if (!empty ($translate[travel_book_now_btn_text])) {
		$travel_book_now_btn_text = $translate[travel_book_now_btn_text];
	}
	else {
		$travel_book_now_btn_text = 'Book Now';
	}

	if (!empty ($translate[travel_more_info])) {
		$travel_more_info = $translate[travel_more_info];
	}
	else {
		$travel_more_info = 'More Info';
	}

	if (!empty ($translate[travel_more_destinations])) {
		$travel_more_destinations = $translate[travel_more_destinations];
	}
	else {
		$travel_more_destinations = 'More Destinations';
	}

	if (!empty ($translate[agency_info_text])) {
		$agency_info_text = $translate[agency_info_text];
	}
	else {
		$agency_info_text = 'Agency\'s Info';
	}

	if (!empty ($translate[search_destinations_text])) {
		$search_destinations_text = $translate[search_destinations_text];
	}
	else {
		$search_destinations_text = 'Search Destinations';
	}

	if (!empty ($translate[number_of_days_text])) {
		$number_of_days_text = $translate[number_of_days_text];
	}
	else {
		$number_of_days_text = 'Number of days';
	}

	if (!empty ($translate[price_range_text])) {
		$price_range_text = $translate[price_range_text];
	}
	else {
		$price_range_text = 'Price Range';
	}

	if (!empty ($translate[search_text])) {
		$search_text = $translate[search_text];
	}
	else {
		$search_text = 'Search';
	}

	if (!empty ($translate[search_results_for_text])) {
		$search_results_for_text = $translate[search_results_for_text];
	}
	else {
		$search_results_for_text = 'Search Results for';
	}

	if (!empty ($translate[no_destinations_text])) {
		$no_destinations_text = $translate[no_destinations_text];
	}
	else {
		$no_destinations_text = '0 destinations found based on your search query. Please, try something else.';
	}

	if (!empty ($translate[travel_price_text])) {
		$travel_price_text = $translate[travel_price_text];
	}
	else {
		$travel_price_text = 'Price';
	}

	if (!empty ($translate[travel_periods_text])) {
		$travel_periods_text = $translate[travel_periods_text];
	}
	else {
		$travel_periods_text = 'Periods';
	}

	if (!empty ($translate[travel_transportation_text])) {
		$travel_transportation_text = $translate[travel_transportation_text];
	}
	else {
		$travel_transportation_text = 'Transportation';
	}

	if (!empty ($translate[travel_transportation_icon_text])) {
		$travel_transportation_icon_text = $translate[travel_transportation_icon_text];
	}
	else {
		$travel_transportation_icon_text = 'Transportation Icon';
	}

	if (!empty ($translate[travel_basic_info_text])) {
		$travel_basic_info_text = $translate[travel_basic_info_text];
	}
	else {
		$travel_basic_info_text = 'Basic Info';
	}

	if (!empty ($translate[travel_short_info_text])) {
		$travel_short_info_text = $translate[travel_short_info_text];
	}
	else {
		$travel_short_info_text = 'Short Info';
	}

	if (!empty ($translate[travel_media_text])) {
		$travel_media_text = $translate[travel_media_text];
	}
	else {
		$travel_media_text = 'Media';
	}

	if (!empty ($translate[travel_upload_text])) {
		$travel_upload_text = $translate[travel_upload_text];
	}
	else {
		$travel_upload_text = 'Upload';
	}

	if (!empty ($translate[travel_destination_text])) {
		$travel_destination_text = $translate[travel_destination_text];
	}
	else {
		$travel_destination_text = 'Destination';
	}

	if (!empty ($translate[travel_new_booking_for_text])) {
		$travel_new_booking_for_text = $translate[travel_new_booking_for_text];
	}
	else {
		$travel_new_booking_for_text = 'New booking for';
	}

	if (!empty ($translate[travel_from_text])) {
		$travel_from_text = $translate[travel_from_text];
	}
	else {
		$travel_from_text = 'from';
	}

	if (!empty ($translate[travel_received_booking_text])) {
		$travel_received_booking_text = $translate[travel_received_booking_text];
	}
	else {
		$travel_received_booking_text = 'We received your booking';
	}

	if (!empty ($translate[travel_received_booking_for_text])) {
		$travel_received_booking_for_text = $translate[travel_received_booking_for_text];
	}
	else {
		$travel_received_booking_for_text = 'We received your booking for';
	}

	if (!empty ($translate[travel_confirmation_message_text])) {
		$travel_confirmation_message_text = $translate[travel_confirmation_message_text];
	}
	else {
		$travel_confirmation_message_text = 'We will contact you as soon as possible to confirm it.';
	}
?>