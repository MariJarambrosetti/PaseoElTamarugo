<?php
	if (!empty ($translations[project_client])) {
		$project_client = $translations[project_client];
	}
	else {
		$project_client = 'Client';
	}

	if (!empty ($translations[project_country])) {
		$project_country = $translations[project_country];
	}
	else {
		$project_country = 'Country';
	}

	if (!empty ($translations[project_date])) {
		$project_date_text = $translations[project_date];
	}
	else {
		$project_date_text = 'Date';
	}

	if (!empty ($translations[project_implementation_time])) {
		$project_implementation_time = $translations[project_implementation_time];
	}
	else {
		$project_implementation_time = 'Implementation Time';
	}

	if (!empty ($translations[project_budget])) {
		$project_budget = $translations[project_budget];
	}
	else {
		$project_budget = 'Budget';
	}

	if (!empty ($translations[project_website])) {
		$project_website = $translations[project_website];
	}
	else {
		$project_website = 'Website';
	}

	if (!empty ($translations[project_gallery])) {
		$project_gallery = $translations[project_gallery];
	}
	else {
		$project_gallery = 'Photo Gallery';
	}

	if (!empty ($translations[project_video])) {
		$project_video = $translations[project_video];
	}
	else {
		$project_video = 'Video';
	}
?>